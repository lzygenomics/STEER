# Core Pipeline

The repository includes an end-to-end **STEER Core Pipeline Demo** notebook for interactive usage.[^tutorials]

## Intended audience

Use this path when:

- your `.h5ad` is already prepared
- `spliced` and `unspliced` layers already exist
- you want the shortest path to a first STEER result

## What this page should contain

Replace this scaffold with four things from your notebook:

1. input file requirements
2. minimal commands to run the model
3. output objects and saved files
4. one representative result plot

## Suggested page pattern

```text
Input data -> load h5ad -> run STEER -> inspect velocity -> save outputs
```

## Link your notebook

Add a direct GitHub link here once you decide which notebook is the official entry point.

[^tutorials]: The README lists a STEER Core Pipeline Demo notebook as the end-to-end example workflow. citeturn259328view1
