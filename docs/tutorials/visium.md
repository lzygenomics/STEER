# 10x Visium Preprocessing

The repository describes the Visium route as a standard workflow bridging **`velocyto`** with **Seurat outputs**.[^tutorials]

## Use this page when

- your data comes from **10x Visium**
- your current objects are still closer to a Seurat workflow
- you need to generate `spliced` and `unspliced` layers before STEER

## Fill in these sections

- required Visium inputs
- where `velocyto` fits
- how Seurat outputs are converted or aligned
- export to `.h5ad`
- run STEER after preprocessing

[^tutorials]: STEER's README explicitly lists a 10x Visium pipeline and summarizes it as a bridge between velocyto and Seurat outputs. citeturn259328view1
